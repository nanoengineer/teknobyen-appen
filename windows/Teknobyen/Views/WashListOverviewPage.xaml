<Page
    x:Class="Teknobyen.Views.WashListOverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teknobyen.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:vm="using:Teknobyen.ViewModels"
    xmlns:models="using:Teknobyen.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:WashListOverviewPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Text="Vaskeliste" HorizontalContentAlignment="Center" >
            <controls:PageHeader.SecondaryCommands>
                <AppBarButton Label="Admin" Click="{x:Bind ViewModel.GotoAdminPage}" />
            </controls:PageHeader.SecondaryCommands>
        </controls:PageHeader>

        <Grid Grid.Row="1" Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Neste vaskedag:" />
            <!-- Neste vaskedag -->
            <Grid Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Style="{StaticResource SubtitleTextBlockStyle}"  Text="{x:Bind ViewModel.NextWashDay.Date.DayOfWeek}" />
                    <TextBlock Grid.Column="1" Style="{StaticResource SubtitleTextBlockStyle}" Text="om" />
                    <TextBlock Grid.Column="2" Style="{StaticResource SubtitleTextBlockStyle}" Text="20 dager" />

                </Grid>
            </Grid>

            <TextBlock Grid.Column="1" Style="{StaticResource TitleTextBlockStyle}" Text="Vaskeliste:" />
            <ListView Grid.Row="1" Grid.Column="1"
                          ItemsSource="{x:Bind ViewModel.WashList}"
                          >
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:WashDayModel" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{x:Bind Date.Date}" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Assignment}" />
                            <TextBlock Grid.Column="2" Text="{x:Bind RoomNumber}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

        </Grid>
        
        
    </Grid>
</Page>
