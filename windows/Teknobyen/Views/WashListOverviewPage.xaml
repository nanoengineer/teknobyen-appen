<Page
    x:Class="Teknobyen.Views.WashListOverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teknobyen.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Text="Vaskeliste" HorizontalContentAlignment="Center" >
            <controls:PageHeader.SecondaryCommands>
                <AppBarButton Label="Admin" />
            </controls:PageHeader.SecondaryCommands>
        </controls:PageHeader>

        <Grid Grid.Row="1" MaxWidth="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Neste vaskedag:" />
            <!-- Neste vaskedag -->
            <Grid Grid.Row="1">
                
            </Grid>

            <TextBlock Grid.Row="2" Style="{StaticResource TitleTextBlockStyle}" Text="Vaskeliste:" />
            <ListView Grid.Row="3">
                
            </ListView>

        </Grid>
        
        
    </Grid>
</Page>
