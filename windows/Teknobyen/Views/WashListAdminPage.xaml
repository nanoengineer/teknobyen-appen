<Page
    x:Class="Teknobyen.Views.WashListAdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Teknobyen.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:vm="using:Teknobyen.ViewModels"
    xmlns:models ="using:Teknobyen.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:WashListAdminPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Text="Vaskeliste administrasjon" HorizontalContentAlignment="Center" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        
            <!-- Current wash list grid -->
            <Grid Padding="5,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Vaskeliste:" Style="{StaticResource TitleTextBlockStyle}" />

                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.WashDayList}"
                          >
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:WashDayModel" >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{x:Bind Date.Date}" />
                                <TextBlock Grid.Column="1" Text="{x:Bind Assignment}" />
                                <TextBlock Grid.Column="2" Text="{x:Bind RoomNumber}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBox Text="{x:Bind ViewModel.SelectedWashDay.Date}" />
                    <TextBox Grid.Column="1" Text="{x:Bind ViewModel.SelectedWashDay.Assignment}" />
                    <TextBox Grid.Column="2" Text="{x:Bind ViewModel.SelectedWashDay.RoomNumber}" />
                </Grid>

                <StackPanel Grid.Row="3" Orientation="Horizontal">
                    <Button Content="Add" />
                    <Button Content="Add" />
                    <Button Content="Add" />
                </StackPanel>


            </Grid>
            
            <!-- Create washlist  -->
            <Grid Grid.Column="1" Padding="5,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBox Text="{x:Bind ViewModel.WashListAsText, Mode=TwoWay}" AcceptsReturn="True">
                    
                </TextBox>
                <Button Grid.Row="1" Content="Parse" Click="{x:Bind ViewModel.ParseWashListText, Mode=OneWay}" />

                <!-- Parsed dates -->
                <ListView Grid.Row="3"
                          ItemsSource="{Binding ParsedWashDayList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          >
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:WashDayModel" >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{x:Bind Date.Date}" />
                                <TextBlock Grid.Column="1" Text="{x:Bind Assignment}" />
                                <TextBlock Grid.Column="2" Text="{x:Bind RoomNumber}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <StackPanel Orientation="Horizontal" Grid.Row="4">
                    <Button Click="{x:Bind ViewModel.SaveParsedList}">
                        <TextBlock Text="Save" />
                    </Button>
                </StackPanel>
                
            </Grid>
        </Grid>
    </Grid>
</Page>
